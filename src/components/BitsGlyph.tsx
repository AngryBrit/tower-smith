/** Inline Bits glyph for guardian chip upgrade costs (`public/icons/Bits.webp`). */
export function BitsGlyph({ className }: { className?: string }) {
  return (
    <span className={className} aria-hidden>
      <img src="/icons/Bits.webp" alt="" width={16} height={16} className="workshop-bits-img" />
    </span>
  )
}
