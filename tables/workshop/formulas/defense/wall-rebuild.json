{
  "name": "Wall Rebuild",
  "workshopKey": "wallRebuildLevel",
  "category": "defense",
  "format": "secondsCooldown",
  "pipeline": [
    { "op": "add", "refs": ["base"] },
    { "op": "sub", "refs": ["reduction"] },
    { "op": "clampMin", "value": 0 }
  ],
  "operands": {
    "base": { "kind": "base" },
    "reduction": {
      "kind": "optsNumber",
      "field": "wallRebuildLabSecondsReduction",
      "default": 0,
      "activeRule": "positive"
    }
  },
  "source": {
    "binaryFunction": "ModuleManager::get_WallRebuild",
    "virtualAddress": "0x01F0AC38",
    "notes": "max(0, workshop seconds - Wall Rebuild lab/relic/submodule reduction). Cooldown format (rounds to 0.1s). Plain (no reduction) shows {n}s via legacy fallback.",
    "verifiedAgainst": "playerInfo.dat -> workshop Wall Rebuild card"
  }
}
