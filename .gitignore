# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.vite

# Environment (secrets)
.env

dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.cursor
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local lab import scratch (not for the repo)
tmp-labs-upstream.json
tmp-labs.tsv

# i18n maintainer scratch (regenerate via scripts/analyze-es-keys.mjs)
tmp-strings-en.json
tmp-kept-en-keys.json

# Local IL2CPP / asset extraction scratch (not for the repo)
tools/

# Sensitive player save dumps (never commit)
docs/player-save-field-dump.json
docs/player-save-field-dump.txt

# Playwright
test-results/
playwright-report/

# Runtime copy of authoritative GOD tables (generated by scripts/copy-god-tables-to-public.mjs)
public/tables/


# Local Supabase incremental migrations (not for the repo)
supabase/migrations/

# Local Netlify folder
.netlify
